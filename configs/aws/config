# AWS default configs for EDToaster
# Meant to be symlinked at ~/.aws/config for Linux/MacOS

[default]
# default region is IAD
region          = us-east-1
cli_auto_prompt = on-partial
cli_history     = enabled
max_attempts    = 1
output          = json